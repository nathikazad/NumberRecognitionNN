This is a fully self contained repository to do number recognition using feed forward neural networks. 
If you have a normal computer type 
make cpu
it will make file call 'nn_cpu'
If you have an OpenMP enabled processor type
make phi
it will make file call 'nn_phi'

